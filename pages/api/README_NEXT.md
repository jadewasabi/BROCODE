# Next.js API route setup

Your frontend uses fetch(`/api/...`). In Next.js, those routes must live under:
- `pages/api/*`

This folder contains the Next.js wrappers/endpoints.

Deploy on Vercel with a Next.js project (framework detects `.next`).

